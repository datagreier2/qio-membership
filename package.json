npm init -y

cat > package.json << 'JSON'
{
  "name": "qio-membership",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "deploy": "netlify deploy --prod --dir=public"
  },
  "devDependencies": {
    "@netlify/functions": "^2.6.0",
    "@types/node": "^22.0.0",
    "esbuild": "^0.23.0",
    "netlify-cli": "^17.34.3",
    "typescript": "^5.6.3"
  }
}
JSON
